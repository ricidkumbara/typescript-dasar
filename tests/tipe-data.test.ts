describe('Data Type', () => {
    it('should be must declare', () => {
        let name: string = 'Ricid Kumbara'
        let balance: number = 100000
        let isVip: boolean = true

        console.log(name, balance, isVip)
    })
})
