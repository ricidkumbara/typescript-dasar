describe('Union Type', () => {
    it('should support in javascript', () => {
        let sample: number | string | boolean = "ricid"
        console.log(sample)

        sample = 100
        console.log(sample)

        sample = true
        console.log(sample)
    })
})